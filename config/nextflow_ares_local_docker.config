process {
    executor = 'local'

    withName: '.*SPACERANGER_COUNT.*' {
        cpus = 8
        memory = 45.GB
    }
}

executor {
    queueSize = 6
}

docker {
    enabled = true
}

params {
    max_cpus = 40
    max_memory = '40.GB'
    max_time = '48.h'
}
