diff --git a/workflow/rules/common.smk b/workflow/rules/common.smk
index 86a734d..df4ccd3 100644
--- a/workflow/rules/common.smk
+++ b/workflow/rules/common.smk
@@ -63,7 +63,9 @@ samples["group"] = [_group_or_sample(row) for _, row in samples.iterrows()]
 if "umi_read" not in samples.columns:
     samples["umi_read"] = pd.NA
 
-validate(samples, schema="../schemas/samples.schema.yaml")
+# Pandas 3.0 fix: reset index before validate to avoid duplicate index error in .update()
+_samples_for_validate = samples.reset_index(drop=True)
+validate(_samples_for_validate, schema="../schemas/samples.schema.yaml")
 
 
 # Does this correctly return groups where fusion and variants are set?

=== Additional hunk (added 2026-04-22 by claude-code-loop-agent): ===
# mapping.smk:207 had nested same-char quotes inside an f-string that old Python
# parsers rejected. Rewritten to use single quotes inside the f-string.

--- /mnt/common/mtang11/hpc_workflows/repos/dna-seq-varlociraptor/workflow/rules/mapping.smk.orig.pre-mtang-patch	2026-04-22 00:22:53.101578966 -0500
+++ /mnt/common/mtang11/hpc_workflows/repos/dna-seq-varlociraptor/workflow/rules/mapping.smk	2026-04-22 00:22:53.133579001 -0500
@@ -204,7 +204,7 @@
         temp("results/consensus/{sample}.consensus.{read_type}.mapped.bam"),
     params:
         index=lambda w, input: os.path.splitext(input.idx[0])[0],
-        extra=lambda w: f"-C {get_read_group("-R")(w)}",
+        extra=lambda w: f"-C {get_read_group('-R')(w)}",
         sort="samtools",
         sort_order="coordinate",
     wildcard_constraints:
